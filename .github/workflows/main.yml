name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: "a6081524-588e-414d-b3d4-c4bcd6109c23"
          heroku_app_name: "techzbotsapi" #Must be unique in Heroku
          heroku_email: "techzcompressor@outlook.com"
